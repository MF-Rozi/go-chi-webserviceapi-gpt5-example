version: "2"

overrides:
  go:
    package: db

sql:
  - engine: postgresql
    queries: db/queries
    schema: db/migrations
    gen:
      go:
        out: internal/db
        package: db
        sql_package: pgx/v5
        emit_pointers_for_null_types: true
